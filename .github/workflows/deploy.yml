name: Deploy

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2     
      - name: deploy action step
        uses: Glyphack/fandogh-service-deploy-action@v0.1.0-alpha
        with:
          username: f
          password: f
          image_name: f
          image_version: 2
          path_to_service_file: ./manifests/service.yml
